# Приложение для анализа банковских операций
## Приложение для анализа транзакций, которые находятся в Excel-файле.Приложение генерирует JSON-данные для веб-страниц, формирует Excel-отчеты, а также предоставляет другие сервисы.
## Установка:
1. Клонируйте репозиторий:
```
git clone https://github.com/Veronika-w/Kursovoy_Project1.git
```
2. Установите зависимости:
```
pip install -r requirements.txt
```
## Основные модули, реализованные в проекте:
- Модуль utils.py. В нем реализован набор функций, формирующих 
информацию по каждой карте, топ-5 транзакций по сумме платежа, курс валют, стоимость акций из S&P500.
- Модуль views.py - принимает на вход строку с датой и временем 
 и возвращающую JSON-ответ с данными из модуля utils.py
- Модуль services.py. Реализована функция, которая позволяет проанализировать, какие категории были наиболее выгодными для выбора в качестве категорий повышенного кешбэка
- Модуль reports.py. Функция возвращает траты по заданной категории за последние три месяца (от переданной даты).

## Тестирование
Для тестирование используется библиотека pytest.

Тестовое покрытие составляет более 80%. Для запуска тестов используется команда pytest
## Логирование
1. Файлы логов записываются в папку logs
2. Для модуля reports.py - reports.log
3. Для модуля services.py - services.log
4. Для модуля utils.py - views.log
5. Файлы логов перезаписываемые